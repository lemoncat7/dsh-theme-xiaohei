# 0.3.5 · 侧栏按钮与思考气泡修复

延续 0.3.4 的 DSH 0.1.5-rc.1 兼容基线，Node.js ^22.19.0 或 >=24.0.0。本次浏览器回归使用桌面及移动端模拟 DOM，未重新完成其他宿主版本的端到端验证。

- 按中英文 aria-label 定位原生收起侧栏按钮，恢复显示和点击，不依赖按钮排列位置，不覆盖宿主明确隐藏或禁用的控件。
- 仅思考内容的助手节点移除气泡背景、边框、留白与头像，恢复普通过程行；混合正文仍保留聊天气泡。
- 不修改 DSH 官方核心代码，不增加运行时轮询。
